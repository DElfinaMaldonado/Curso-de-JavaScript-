let animales = ['cerdo', 'caballo']; // → crear el array
console.log(animales); // → imprimir el array
console.log(animales[0]); // → para traer solo el valor del indice 0
animales[2] = 'vaca'; // → agregar elementos al array
console.log(animales);

console.log(typeof animales); // → Nosad el tipo de dato, en este caso en un objeto.


console.log(animales.length); //Devuelve la cantidad de elementos que se tienen en el array